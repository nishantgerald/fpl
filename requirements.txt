Flask==3.1.0
Flask-CORS==4.0.0
Requests==2.32.3
cryptography==50.0.0
gunicorn==23.0.0
numpy==2.1.3
openai==1.84.0
scipy==1.18.0
# Scoring only, not training: `ml/predict.py` loads a persisted artifact and
# calls predict on it. Without these the dyno cannot read the model at all, and
# /api/engines answered "No trained model artifact on this server" while the
# artifact sat in the repo. ~59 MB of slug for the engine to exist.
# Pinned to what the artifact was fitted with — joblib refuses a mismatch it
# cannot unpickle, and a silent version drift here is a silent fallback.
scikit-learn==1.5.2
joblib==1.4.2
psycopg[binary]==3.2.3
python-dotenv==1.0.0
pytest==8.3.4
